{
  "name": "@3ac.vc/cow-dca-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@7.14.2",
  "contributors": [],
  "scripts": {
    "build": "turbo run build",
    "build:app": "turbo run build --filter=dca-sdk --filter=dca-app",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "lint:commit-message": "commitlint -e",
    "format": "turbo run format",
    "husky": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "@commitlint/config-conventional": "^17.4.2",
    "eslint-config-custom": "workspace:0.0.0",
    "husky": "^8.0.3",
    "prettier": "^2.8.3",
    "turbo": "^1.7.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
